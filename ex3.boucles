#include <stdio.h>

int main() {
    int a, b;
    printf("Entrez deux entiers : ");
    scanf("%d %d", &a, &b);

    if (a == b)
        printf("Résultat : %d\n", (a + b) * 3);
    else
        printf("Résultat : %d\n", a + b);

    return 0;
}
